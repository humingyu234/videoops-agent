<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.aivideo.workflow.mapper.RunningHubAccountMapper">
    <select id="selectAdminPage" resultType="org.dromara.aivideo.workflow.domain.RunningHubAccount">
        SELECT account_id, tenant_id, account_name, api_key_ciphertext, api_key_masked, enabled,
               last_health_status, last_health_time, last_health_summary, credential_updated_at,
               row_revision, create_by, update_by, del_flag, create_time, update_time
        FROM av_runninghub_account
        WHERE tenant_id = #{tenantId} AND del_flag = '0'
        <if test="query != null and query.keyword != null and query.keyword != ''">
            AND account_name LIKE CONCAT('%', #{query.keyword}, '%')
        </if>
        <if test="query != null and query.enabled != null">
            AND enabled = #{query.enabled}
        </if>
        ORDER BY update_time DESC, account_id DESC
    </select>
</mapper>
