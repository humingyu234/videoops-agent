# 当前施工状态

本文件是当前状态、运行证据、阻塞和现场动作的唯一实时来源。静态阶段路线见 `docs/PLAN.md`；目标、边界、验收和执行命令见本文件点名的当前辅助 runbook。

## 当前快照

| 字段 | 值 |
| --- | --- |
| 当前阶段 | T7：`RUNTIME MIGRATION DONE / REUSE ENTRY PRECHECK READY / RUNTIME E2E BLOCKED`；project/render 复用已在创建任何 AgentRun 合同前完成 owner/status/revision/type 只读校验，真实复用运行仍等待可用的开发库运行身份 |
| 当前详细计划 | `docs/superpowers/plans/2026-08-16-videoops-agent-t7-product-entry.md`（当前唯一辅助 runbook） |
| 状态 | `BLOCKED` |
| 风险等级 | `RED`：默认入口将直接驱动真实 AgentRun、批准和成品下载；身份、幂等、恢复或 Trace 误报都属于执行 spine |
| 当前核对源码 | `branch:main; base/origin-main:578b2567a8035244bdbd57ee0ec0f3be1ffa50fb; current precheck checkpoint:本提交; T6 checkpoint:45926f642c3d0ca54b22d6fac6044dd554fb7425; T5 checkpoint:ec8f288dca73d70ea50c16957e88fbbd6bce00b2; T4 checkpoint:a66c7cfbbb0de4c75a75c9cfc82deee442d84ccc` |
| 工作区 | 本轮从 clean checkpoint `578b2567a8035244bdbd57ee0ec0f3be1ffa50fb` 补齐 project/render 创建前预检并收口为 clean checkpoint；README 由监督线程维护，本线程不修改、不 push、不改远端 |
| 最近有效运行证据 | `T7-E05`：project/render 正例在创建合同前复用 owner-scoped Service；错误 revision、非 success render 和错误任务事实均以 46704 在首个 Brief 写入前拒绝。运行账号恢复的两条受控解析方法都在 MySQL 连接前停止，DPAPI 载体时间未变，因此真实 18081 复用仍为 `NOT_RUN` |
| 最后更新 | 2026-08-17（Asia/Shanghai） |

状态只使用 `NOT_STARTED`、`IN_PROGRESS`、`VERIFYING`、`BLOCKED`、`DONE`、`NEEDS_REVALIDATION`、`DEFERRED`、`PAUSED`。`BLOCKED` 仅表示当前范围内已经没有安全动作可继续；`PAUSED` 表示负责人要求在已完成切片后停止，不代表技术阻塞。

## T1 步骤现场

| 步骤 | 状态 | 当前证据或阻塞 | 完成信号 |
| --- | --- | --- | --- |
| T1.0 开工与授权门禁 | `DONE` | `T1-E01`：项目负责人已要求从 T1.0 开始并允许继续安全检查；路径、分支、HEAD、clean worktree、仓库标记、runbook 指针和开发规范均已实时核验。当前只授权免费、只读、不调用 Provider 的检查；测试账号、固定素材、Provider 和额度上限尚未授权，真实付费调用保持禁止 | 实时记录路径/分支/HEAD/工作区并确认进入 T1；付费调用前置授权被明确记录 |
| T1.1 本机环境与端口 | `DONE` | `T1-E02`/`T1-E03`：Java 21.0.12 通过单进程临时注入可解析；Node 24.14.1、npm 11.11.0、FFmpeg/ffprobe 8.1.1、CJK 字体、3306/6379 均通过。39000/8189 未监听，记为后续 Provider 缺口；8080 由 `D:/project/ai-video` 的既有 Java 后端监听且未触碰。18080 是既有 PortProxy；项目负责人批准改用 18081，已证明无监听、无 PortProxy、不在系统排除范围且可绑定后释放 | Java 21、Node、npm、FFmpeg/ffprobe、CJK 字体满足要求；3306/6379 可达；18081 空闲；不终止既有 8080 进程 |
| T1.2A 独立运行基线 | `DONE` | `T1-E08`：独立 dev/launcher、Redis DB14 + 双前缀、18081、独立目录和 marker 已落地。`T1-E09`：当前黄金链最小 OSS 子门禁通过。`T1-E10`：独立 MySQL bootstrap、最小权限账号、纯合成 seed 和旧状态反向对账通过。`T1-E11`：旧 8080 保持运行时，当前应用只连接新库/DB14；停止旧 8080 未获授权、未实际执行。RunningHub、管理端 CRUD、通用 OSS 治理仍不属于本切片 | 配置、数据和存储边界不依赖旧 8080；有效配置不含 `ai_video`/Redis DB 0/空前缀/旧目录；新库可由 manifest 冻结包重建；当前 T1 黄金链的存储写入被服务端限定到独立 namespace |
| T1.2 数据库、Redis 与迁移 | `DONE` | `T1-E10`：唯一 manifest 固定 `001..090 → 900`；新库由空库建立，结构和 seed postcondition、seed 幂等、专用账号正向/旧库负向、旧库与 Redis 前后快照均通过。T1.2 当时的 seed 为 29 行；T1.3 为创作账号补入 2 项权限与 2 条绑定后，当前冻结 seed 为 33 行且 clean bootstrap 可复现。`T1-E06` 仅保留为共享库历史诊断，禁止据此继续施工 | 独立目标库明确；基础结构、精确迁移与最小脱敏 seed 可从空库重建；Redis 仅命中 DB14/项目前缀；未写入旧项目状态 |
| T1.3 账号、真实资产与 OSS | `DONE` | `T1-E12` 保留早期失败与 JVM 内 client 修复证据；`T1-E13` 已完成恢复：版本化 seed/manifest 与现场权限均为 12 项，当前账号经现有 API 获得一个 owned + ready 人物和一个 owned + origin/unparsed 原声。两条对象记录均在精确项目 namespace，列表和授权读取通过；OSS 凭据只在本轮子进程内使用，`sys_oss_config=0`、DB14 OSS 配置键为 0。全部 Provider 仍关闭且 `NOT_RUN` | 当前账号有创作权限；至少一个归属正确的 `ready` 人物、一个 `origin` 原声音及其 OSS 对象可经授权读取 |
| T1.4 Provider 与工作流身份 | `DONE` | `T1-E14`：当前项目 Adapter 直连 company-dev-gpu，不经过旧 8080；ComfyUI 工作流只读一次并冻结 UUID `8b7a9a57-2303-4ef5-9fc2-bf41713bd1fc`、JSON SHA-256 `E0BA941A6545FCFDDB6D5E580726441872680CB3739359E66F33AD362B5BFE21`、实际主模型/MultiTalk/LoRA 身份。IndexTTS2 与 ComfyUI 在唯一真实任务中均成功，external task/prompt 身份已持久化；未为诊断额外生成媒体 | Provider 认证/连通性成立；实际工作流 UUID、JSON SHA-256、模型加载节点和环境标签已记录；未为诊断重复生成媒体 |
| T1.5 后端测试、构建与启动 | `DONE` | `T1-E11`：launcher 固定 dev、loopback:18081、新库/DB14/双前缀和独立目录；该次运行显式 OSS=false，其余外部能力关闭。launcher 默认 OSS=false，但负责人可在后续独立授权轮显式开启。专项测试、打包、真实启动、健康、登录、权限、积分和人物/声音只读路由通过；`T1-E14` 又以当前 launcher 完成真实 `/studio` 登录链，历史 ACL 后未全链复跑风险已闭合；当前进程已停止 | 专项测试和构建通过；用户 API 在 18081 启动；健康、登录、权限和目标业务路由通过 |
| T1.6 无 Mock 前端验证 | `DONE` | `T1-E14`：真实 Web `/studio` 使用 18081 API 登录，显示并选中 T1.3 人物与原声；固定文案经页面提交，异步状态恢复后页面显示真实“当前成品”并触发下载。前端任务适配器已兼容当前 `AiTaskVo` 字段与显式 null，未使用 Mock 或旧 8080 | 专项测试通过；Web 使用 18081 真实 API；登录、创作权限、真实人物/声音列表及加载/失败状态可验证 |
| T1.7 单次真实黄金样例 | `DONE` | `T1-E14`：同一意图仅创建 voice job `2088530227838955521`、video job `2088530778710454273`、project `2088539060225290242`、render task `2088539141703839746` 与 output asset `2088539145776508930` 各一条；全部成功且因果链一致。最终 MP4 已从页面授权入口下载，25.8 秒、9:16、有 AAC 音轨与可见烧录字幕 | 一个固定约 30 秒中文、9:16、带字幕意图只提交一次；记录 voice/video job、project、timeline task、output asset、幂等身份、耗时和失败点 |
| T1.8 成品与一致性验收 | `DONE` | `T1-E15`：新 project `2088580138567495681`、render task `2088580140291354626`、output asset `2088580141574811650` 因果一致。新 MP4 完整解码、9:16、有 H.264/AAC 双流，音视频起止差均为 0；黑场、冻结、静音检测均为 0。字幕改为 8 段并增加黑色 3 px 描边，人工观感改善；底片与声音复用 T1.7 资产，Provider job 总数仍为 2、无新 Provider 调用 | `outputs/latest` 与任务详情一致；MP4 可解码、9:16、有音轨和可见字幕；SHA-256、ffprobe、0.25 秒音视频不变量、人工检查和实际工作流身份均有证据 |
| T1.9 证据、独立复核与收口 | `DONE` | `T1-E16`：当前源码后端 17 类 155/155 与 package、前端 8 文件 45/45 + lint/tsc/build、launcher Pester 12/12、bootstrap Pester 16/16、bootstrap validator、开发规范、staged diff/秘密/媒体门禁均通过；新库/DB14/namespace、旧库拒绝、DB0/旧 8080 与端口完成最终对账。独立审查结论为本机/试运行 checkpoint 可接受，未发现 P0/P1 | 每项记录 `PASS`、`FAIL` 或 `NOT_RUN`、源码状态、真实性标签、命令/任务/产物和剩余风险；非实施者确认因果链、无重复收费提交、模型声明和秘密脱敏后才可 `DONE` |

## T2 步骤现场

| 步骤 | 状态 | 当前证据或阻塞 | 完成信号 |
| --- | --- | --- | --- |
| T2.1 短 runbook 与范围冻结 | `DONE` | 当前 runbook 固定三表、core Service、迁移与两层边界测试；明确不做 UI、Planner、工具包装或任务重写 | 文件面、非目标和三个验收信号明确 |
| T2.2 版本化 Brief/Profile | `DONE` | 不可变版本、owner 归属、规范 JSON 摘要与幂等回放已实现；单元边界通过 | 版本追加不覆盖旧行；跨 owner 拒绝；异摘要冲突 |
| T2.3 AgentRun 状态与恢复 | `DONE` | 同一 run 的 queued/running/waiting 领取、租约代次、重启恢复与终态不可逆已由 Service 单元和真实 MySQL IT 共同验证 | 同一 run 可由新 Service 实例恢复；终态不可逆 |
| T2.4 迟到结果隔离 | `DONE` | owner、row version、lease generation/token digest、task identity 与 contract revision 均进入 CAS；等待完成、失败和取消原子核对外部任务及候选资产事实。旧 token、旧 task、旧 contract revision、错误 owner/任务/资产与重复终态在真实 MySQL 中均影响 0 行 | 旧 token、旧 task、旧 contract revision 及错误任务/资产均影响 0 行 |
| T2.5 迁移与边界验收 | `DONE` | manifest/validator、Pester 16/16、9/9 单元测试、当前源码真实 MySQL IT 3/3、开发规范、diff 与秘密门禁全绿；`ai_video_test` 非破坏性复用，随机测试表清零，专用账号只具该库六项权限 | manifest/validator、单元测试、真实 MySQL IT、规范与 diff 全绿 |

## T3 步骤现场

| 步骤 | 状态 | 当前证据或阻塞 | 完成信号 |
| --- | --- | --- | --- |
| T3.1 类型化契约与白名单 | `DONE` | `AgentToolDTOs` 与 `IAgentToolService` 冻结 8 把黄金链工具；未知工具、缺失/错误/多余参数均在委托前稳定拒绝 | 只暴露 T1 黄金链所需的结构化输入/输出，不含 owner、路径、签名 URL、lease、token 或秘密 |
| T3.2 权限、principal 与 ownership | `DONE` | canonical personal workspace、精确权限和正 ID 先于现有 Service 校验；tenant/owner/workspace 只来自 `AppPrincipalSnapshotDTO`，跨 owner 当前真实读被拒绝 | 权限不足、非法 principal 与跨 owner 均在创建任务前失败 |
| T3.3 黄金链 Service 适配 | `DONE` | 声音提交/确认/状态、数字人提交/状态、项目准备、渲染、渲染状态和成品检查均复用既有 Service；未自 HTTP、未新建表或任务模型 | 每把工具保留既有幂等、任务和资产事实，不建立第二套生成链 |
| T3.4 渲染幂等安全回放 | `DONE` | owner + key + project + revision + digest 的窄预检在媒体读取前命中；同输入返回同一根任务，异参数为 46609，未命中才进入原创建链 | 安全回放不读取 OSS、不新增根任务/execution；首次提交仍走原有校验与 FFmpeg 链 |
| T3.5 当前边界验收与 checkpoint | `DONE` | `T3-E01` 保留真实只读回放与成品对账；`T3-E02` 进一步以 task/resultAsset 单次 owner-scoped JOIN 取代可变 latest，失败任务透传平台已清洗并持久化的稳定 code/message；82/82 聚焦测试、package、开发规范、diff 与秘密门禁全绿 | 当前源码、任务、owned ready output 与本地 MP4 身份一致；历史任务不受项目后续渲染影响；失败原因可供后续 Agent 稳定决策 |

## T4 步骤现场

| 步骤 | 状态 | 当前证据或阻塞 | 完成信号 |
| --- | --- | --- | --- |
| T4 可恢复黄金链编排 | `DONE` | `T4-E01` 保留一般恢复历史证据；`T4-E02` 已补齐精确合同：generation 1 为初次 claim 基线，`maxResumeAttempts=1` 时允许一次 fresh 恢复、下一次才耗尽；Provider adapter 已接受而 waiting CAS 未落库时，fresh instance 以同一幂等键恢复同一根任务，adapter、AgentRun 与生成根任务均为 1 | 结构化计划、跨实例同 task 恢复、单次提交、预算/取消/迟到结果与 owned ready output三组信号全部成立 |

## T5 步骤现场

| 步骤 | 状态 | 当前证据或阻塞 | 完成信号 |
| --- | --- | --- | --- |
| T5 三层逐项质量验收 | `DONE` | `T5-E02` 已闭合独立验收三项缺口：环境/存储不可用为低置信 REVIEW，确证媒体损坏为高置信 FAIL；保存、对齐、ASS 与质量入口共享语义标点规则；真实 MySQL Mapper/Service + 本地媒体边界已验证 owner/task/resultAsset/sourceRef 正反关联 | 当前 T1 MP4 健康解码，截断同字节流不再假 PASS；失效 257 行 LocalIT 与临时 248 行证明夹具均不进入 checkpoint |

## T6 步骤现场

| 步骤 | 状态 | 当前证据或阻塞 | 完成信号 |
| --- | --- | --- | --- |
| T6 有界局部返工与批准 | `DONE` | `T6-E01`：质量候选、独立返工计数与批准事实已持久化；字幕失败只重建 project+render，媒体失败只重 render，最多两个返工候选；无改善、预算耗尽和主观项进入人工批准。真实 MySQL 恢复、崩溃窗、权限/owner/revision 与迟到写回门禁均通过 | 字幕失败只创建同一 video job 的新项目与 render；媒体失败只重 render；最多两个返工候选；无改善/预算/主观项转人工；三类批准精确且可恢复 |

## T7 步骤现场

| 步骤 | 状态 | 当前证据或阻塞 | 完成信号 |
| --- | --- | --- | --- |
| T7 默认入口、Trace、演示与公开交付 | `BLOCKED` | 当前前后端合同、owner-scoped durable Trace、`/agent` 默认路由、迁移和构建均存在；`voice_job/video_job/project/render_task` 显式复用入口默认不影响 `new`。voice/video 冻结 owner-scoped input hash；project/render 又在任何 Brief/Profile/Run 写入前核 owner、类型、状态、revision 与精确成品关联。当前运行身份仍不可用，未能实时对账 T1 持久事实或启动本轮 18081，因此复用入口真实 E2E、成功成品、局部返工和 final approval 仍未到达，不能宣称 T7 DONE | `/agent` 无 Mock 运行；成功、局部修复、转人工、重启恢复四场景可复现；README、许可证、扫描、测试、视频与同一 checkpoint 一致 |

## 已知风险与前置缺口

- Java 21.0.12 当前可由 `java.exe` 直接解析；T7 当前源码聚焦测试、package 和 18081 启动均已使用该运行时通过，不再保留“Java 不在 PATH”的陈旧描述。
- 本机 IndexTTS2 `39000` 与 ComfyUI `8189` 仍未监听；`T1-E14` 复用远端 company-dev-gpu 并由当前 Adapter 完成了唯一真实 voice/video 任务，因此无需把公司 GPU 服务重建到本机。私有地址和认证未进入仓库或证据。
- `8080` 已确认是旧公司项目后端，本任务未停止也未调用它；`T1-E11` 已在其保持运行时证明 VideoOps 只连接新库/DB14。因未获停止旧服务授权，旧 8080 停止态没有动态复跑；独立 datasource、最小权限 1142 拒绝和运行连接归属证明当前应用不以旧实例为运行依赖。
- 共享 `ai_video` 的 T1-E06 只保留为历史诊断证据，不能支持当前数据库完成声明。T1-E10 仅按负责人明确授权把旧 dev 管理员凭据在本机子进程内一次性用于新库 bootstrap，后续运行不依赖该身份，也未对共享库执行 DDL/DML；其生命周期由负责人管理，不构成本轮阻塞。
- 独立 `videoops_agent_dev` 已由 manifest 冻结的 `001..090 → 900` 包从空库建立。`ry_vue.sql`、旧 25 份混合迁移和旧 2 MB 广种子只保留为来源审计，仍不得作为当前新库执行入口。
- T7 的 AgentRun HTTP 入口依赖 manifest 中的 `100 → 110 → 120`。负责人已在受控管理会话中对 `127.0.0.1:3306/videoops_agent_dev` 完成三步；当前 `120`（SHA-256 `F119D4F5ABDA690285B2C7600DC46BFA797DA5571BD5887951E6B87F2976AF90`）自身 validator 已通过，AgentRun 表=5、quality columns/tables/indexes/checks=`3/2/5/12`、normalized null checks=2。旧 `ai_video` 表数前后均为 53；本轮禁止再执行 100/110/120/900，仍不得改写旧 8080、`ai_video`、Redis DB0 或用测试库冒充。
- T7 真实运行暴露声音恢复边界：`DigitalHumanGenerationServiceImpl` 先把 voice job 置为 `running/voice_synthesizing`，再由进程内 `applicationTaskExecutor` 执行 `IndexTts2Client` 的同步 HTTP 请求；该 Provider 合同不返回可持久化 job id。18081 在请求返回前重启后，新实例只能读取原 job 并等待 10 分钟总门禁，不能对账外部结果，也不能安全重提。当前 run 已按 `GENERATION_TIMEOUT` fail-closed 转人工；在持久 Provider 幂等身份/查询能力缺失或负责人重新授权一次生成前，成功、局部返工和最终 MP4 全链保持阻塞。
- T7 本轮按授权尝试恢复 localhost `videoops_agent`，但两条独立受控解析路径都在任何 MySQL 连接前停止：Spring YAML loader 固定 `STAGE=11`，项目已安装标准 `yaml` 解析器固定 `STAGE=12`（精确 master 候选不满足本机 root 形状）。同一方法均未继续堆诊断；账号、grant 与 Git 忽略 DPAPI carrier 均未修改，carrier 仍为 2,304 bytes、UTC 修改时间 `2026-08-14 16:20:33`。因此本轮没有启动 18081，也没有读取开发库业务行或调用 Provider/OSS/Redis。
- Redis DB 14 当前 `DBSIZE=1`，唯一项目 marker 属于 `videoops-agent:dev`、foreign=0，DB0 前后均为 2；DB 15保留给集成测试。源码审计表明 Redisson NameMapper + Sa-Token 双前缀可覆盖现有键写入，但 Pub/Sub 跨 DB，因此非空全局前缀仍是硬门禁。
- `T1-E09` 只闭合当前 T1 dev 黄金链的最小 OSS 代码边界；`T1-E13` 真实验证人物/声音，`T1-E14` 又真实验证数字人登记与 Timeline 成品均经项目 JVM client 写入 `videoops-agent/dev/`。通用 RuoYi OSS 工厂、RunningHub 和管理端 CRUD 仍未迁移，不能把本次结论外推到未运行入口。
- 本地 output 目录仍只是下载/证据落点，不是自动 final output-root；`T1-E14` 已把本次最终 MP4 从授权入口下载到该目录，线上权威记录仍是新库中的 output asset 与项目 OSS namespace。
- 本轮只冻结了 T1 本机 dev/user launcher；两端 `application-prod.yml` 仍可能回落 Redis DB0/空前缀，管理端直接启动端口与集成测试库命名也尚未完成 runtime fork。它们不属于本轮启动入口，但在全局独立性验收前必须改为项目专用且 fail-closed。
- 公司 GPU、ComfyUI、IndexTTS2、模型权重、LoRA 和工作流允许作为外部 Provider 复用；必须由当前项目 Adapter 直接调用并保存 external task/prompt ID，不经过旧 8080，也不回写旧库。
- ComfyUI 上传子目录已在 Adapter 最近边界固定并校验为 `videoops-agent/dev`；非法、旧值和逃逸值会在网络请求前失败。`T1-E14` 的唯一视频任务使用该项目子目录，未混用旧 `digital-human` namespace。
- 明确许可的人物 PNG 与 17.58 秒 MP3 已通过项目 namespace 各写入一次；版本化 `900`/manifest 与现场账号已同步为 12 项权限。JVM 内 OSS client 使本地 override 不再把完整配置写入 Redis；本次运行中 `sys_oss_config=0` 且 DB14 OSS 配置键为 0。旧数据库 OSS cache 路径和尚未迁移到项目 client 的后续存储调用仍是对应阶段启用前的边界，不在本轮扩张。
- Studio 步骤 0～4 的关键上下文主要在 React 内存，创建 `projectId` 前刷新会丢失任务上下文；T1 运行时不刷新，并记录为后续恢复边界。
- 当前数字人链使用 `av_dh_generation_job`，时间轴渲染使用 `av_ai_task`；证据必须分别记录，不能描述成已经统一。
- `T1-E14` 已从实际工作流冻结并记录 MultiTalk `Wan2_1-InfiniteTalk-Single_fp8_e4m3fn_scaled_KJ.safetensors`、主模型 `aniWan2114BFp8E4m3fn_i2v480pNew.safetensors` 与 LoRA `lightx2v_T2V_14B_cfg_step_distill_v2_lora_rank16_bf16.safetensors`。该身份只绑定本次 workflow JSON SHA，不能外推为 Provider 全局版本。
- 当前 `/studio` 轮询只走任务详情，已修复普通 Timeline 详情对 RunningHub `av_workflow_order` 表的无条件依赖并由真实 MySQL 请求验证；任务列表查询仍保留旧 workflow 依赖，因本次页面未经过而 `DEFERRED`。Web dev server 本次监听所有本机地址而非仅 loopback，也记为后续本地启动边界，不影响已完成的 18081 后端 loopback 约束。
- T1.7 收工日志中的 `ECONNREFUSED` 发生在后端先于 Web 停止、页面仍执行既有轮询的窗口；当前轮询在任务终态会清理 timer，因此按收工顺序噪音处理，不扩张产品代码。后续固定先离开或停止 Web 并确认轮询结束，再停止 18081。
- T2 本机 IT 非破坏性复用既有 `ai_video_test`：原 `av_ai_task`、`av_ai_task_execution`、`av_ai_task_attempt` 三表保持不变，测试只使用带随机后缀的隔离表并在每例结束精确清理。`videoops_agent_t2_it` 仅有该 schema 的 `CREATE/DROP/ALTER/SELECT/INSERT/UPDATE/DELETE`；`ALTER` 只用于当前迁移 CHECK 的真实验证，仍无全局、旧开发库或 Grant Option 权限。凭据保存在 Git 忽略、当前用户 ACL 的 DPAPI 载体。负责人已明确接受此前管理秘密只进入私有任务工具输出的风险并决定无需轮换；该值未进入仓库、命令参数、日志或持久环境，本轮不再检查或输出。
- T2 的 completed 记录只在写入瞬间原子确认候选资产为当前 owner 的 ready、未删除视频输出；后续资产删除入口尚未增加“已被 AgentRun 引用”保护，属于使用该终态资产前必须处理的后续生命周期边界，不能把当前候选引用描述为永久可用。
- T3 的历史 task-output 关联已由单条 owner-scoped task/resultAsset JOIN 收口，不再依赖项目 latest；本轮未连接真实 MySQL 执行该新增 SELECT，当前证据是 Mapper SQL 契约、Service 正反边界与同源码 package。生产发布前仍需在受控运行窗口补一次真实历史任务查询，不能把当前 mock/静态边界外推为生产证据。
- T3 工具按 Unicode code point 限制口播文案，下层现有数字人 Service 仍按 UTF-16 `length()` 限制；极端 astral 字符输入可能在工具校验后由下层稳定拒绝。固定中文样例不受影响，当前不为未经过主链的字符边界扩张实现。
- T4 的 `waiting_input` 只冻结当前缺输入事实；补充输入时通过新版本 Brief/Profile 创建新 run，不在原 run 上改写不可变合同。当前本机/试运行链已 fail-closed，但“同 run 原地澄清后继续”尚未提供产品入口。
- `T4-E01` 的恢复链使用真实 MySQL 与生产编排/持久 Service，但外部工具采用跨 context 共享、幂等计数 fake；它证明同一持久 task 恢复、提交接受次数与迟到结果边界，不等同于本轮重新调用真实 Provider。随机 IT 表直接采用 `100 + T4` 的最终结构，`110` ALTER 本身只经过静态 bootstrap 门禁，尚未在既有 `videoops_agent_dev` 表上执行；真实 Provider、OSS、Redis 与 HTTP/UI 均 `NOT_RUN`。
- `T4-E02` 的崩溃窗穿过真实 `AgentToolServiceImpl`、资源生成 Service、生成 Service 与 MySQL，仅最外层声音 Provider adapter 为计数 fake；owned 输入和媒体落点为测试夹具，未访问真实 OSS。它证明 Provider 接受后的幂等恢复合同，不等同于真实 Provider 或 OSS 重跑。user 模块全量 Surefire 因既有、非本切片 `AppSecurityFailClosedConfigurationTest` 失败而未作为本轮门禁；本轮只运行与当前差异匹配的 33 个聚焦单元及 6 个真实 MySQL IT，不为无关失败扩张修复。
- T5 不配置 must/prohibited 词表时，两项按 `policy_unconfigured` 返回低置信 `REVIEW`；人物身份、口型、声音一致性、视觉稳定与语气风格同样保留人工 `REVIEW`。本阶段未引入 OCR、ASR、人脸或口型模型，不能把联系表、静态帧或媒体可解码冒充这些主观事实已通过。

## 证据索引

| ID | 支持的验收项 | 环境/真实性 | 源码状态 | 命令或产物 | 结果 |
| --- | --- | --- | --- | --- | --- |
| T0-E01 | 脱敏快照来源与无历史导入 | 本地仓库 | `commit:02384829b273a5c060147d0f013bd9a37ac00b42` | `docs/BASELINE.md` 与 Git 提交 | `PASS`：仅证明基线建立，不代表 T1 可运行 |
| T0-E02 | 项目迁移到独立仓库 | 本地仓库 | `commit:cab31a0d16fa3f2a1a2f65d9fbff9fd8d117905a` | `docs/BASELINE.md` 中的历史迁移记录 | `PASS`：当时使用 D 盘路径，不构成公开克隆路径要求，也不代表 T7 可发布 |
| T1-E00 | T1 开工前历史只读预查 | 本机开发环境 | `HEAD:cab31a0d16fa3f2a1a2f65d9fbff9fd8d117905a; worktree:clean` | 端口、命令、配置和源码链路清点 | `NOT_RUN`：只保留前置线索，环境事实须按当前现场重验 |
| T1-E01 | T1.0 开工基线、当前 runbook 与授权边界 | 本地仓库；未调用 Provider | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; worktree:clean` | `git rev-parse --show-toplevel` → `D:/project/videoops-agent`；`git branch --show-current`、`git rev-parse HEAD`、`git status --short`；根目录 `.git`/`AGENTS.md`/`docs/PROJECT.md` 均存在；`EXECUTION.md` 与当前 runbook 指针一致；`scripts/validate-development-standards.ps1` → `DEVELOPMENT_STANDARDS_OK`，exit 0 | `PASS`：正式进入 T1，仅继续免费安全检查；账号、素材、Provider 地址/启动方式和额度上限均未授权，真实生成 `NOT_RUN` |
| T1-E02 | T1.1 本机工具、字体与端口门禁 | 本机开发环境；只读 TCP/进程检查；未调用 Provider | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; dirty:docs/EXECUTION.md（预期证据更新）` | `Get-Command java,node,npm`；版本命令；Java 21 绝对路径与单进程临时 `JAVA_HOME`/PATH 注入；`Test-NetConnection 127.0.0.1 -Port 3306,6379,39000,8189,8080,18080`；FFmpeg/ffprobe 版本；`SpecialFolder.Fonts` 回退字体枚举；`Get-NetTCPConnection`、`Get-Process`、`Win32_Service` 只读监听归因 | `FAIL`：18080 由 `IP Helper` 监听，未满足空闲门禁。`PASS`：Java 21.0.12 临时解析、Node 24.14.1（满足 `>=22`）、npm 11.11.0、FFmpeg/ffprobe 8.1.1、微软雅黑/黑体、3306/6379；`NOT_RUN`：Provider 请求、HTTP 请求、进程停止/重启、T1.2 及真实生成。39000/8189 未监听；8080 既有 Java 监听保持不变 |
| T1-E03 | T1.1 备用用户 API 端口选择 | 本机开发环境；未启动项目服务；未调用 Provider | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; dirty:docs/EXECUTION.md（预期证据更新）` | `Get-NetTCPConnection -State Listen -LocalPort 18081`；PortProxy 规则脱敏匹配；IPv4/IPv6 TCP excluded range；`.NET TcpListener(Loopback, 18081)` 临时绑定后立即释放并复查 | `PASS`：18081 无监听、无 PortProxy、不在排除范围、可绑定且已释放；项目负责人明确批准改用 18081。既有 8080/18080 均未修改 |
| T1-E04 | T1.2 数据库、Redis、迁移与并行实例安全门禁 | 本机开发环境；只读协议/进程/配置/文件核验；未执行 SQL；未调用 Provider | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; dirty:docs/EXECUTION.md + current runbook（预期文档更新）` | `RULES.md`、开发库初始化说明、最终 `application-dev.yml` 的脱敏目标解析；本机监听/进程/版本；`mysqladmin --protocol=TCP ... ping`、无凭据只读 MySQL 查询、`redis-cli ... PING`；`Get-FileHash`、行数与 SQL 静态语句/守卫计数；Java 监听枚举；双实例配置与 scheduler/lease/CAS 源码只读比对 | `PASS`：原生 MySQL 8.4.9/Redis 7.2.14，本机目标为 `3306/ai_video` 与 `6379/DB 0`，MySQL 协议 ping exit 0，Redis `PONG`；初始化 SQL 1101 行、2092081 bytes、SHA-256 `744712B4EAF7AA1EF54CD581F53380FE03823ECE894F72206811E6ECBAD57C18`，破坏性语句计数为 0 且有结构守卫，点名迁移文件存在并记录哈希；当前仅观察到 8080 用户端 Java 后端，未观察到第二个 Java 后端。`FAIL`：未准备安全凭据时 MySQL 只读查询 exit 1；初始化说明漏列守卫依赖的两份 20260812 迁移。`NOT_RUN`：实际 schema/选中库/迁移应用、初始化首次与重复执行、任何 SQL 写入、进程停止、服务启动、Provider 请求。共享库下仅换 18081 不能证明任务来自当前 HEAD，但不阻止只读 schema 核对 |
| T1-E05 | T1.2 顺序纠正、安全凭据前置与迁移文档对账 | 本机开发环境；只读配置/文件核验；未读取旧仓密码；未执行 SQL；未调用 Provider | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; dirty:docs/EXECUTION.md + database initialization guide + current runbook（预期文档更新）` | 实时重验仓库/分支/HEAD/status、`EXECUTION.md` 指针、开发规范；仅统计新仓 user-api/ruoyi-admin 密码配置类型与环境变量存在性；`Test-Path` 核验 MySQL 8.4 `mysql_config_editor.exe` 和 login 文件；静态映射初始化守卫与 `20260812_01`/`20260812_03` | `PASS`：仓库仍为 `main@e999b1f2ce1f3d62168c3668de22b0fa6f8f9159`，指针一致，`DEVELOPMENT_STANDARDS_OK`；新仓数据库密码为环境变量引用，当前主数据源密码在 Process/User/Machine 均未设置；`mysql_config_editor.exe` 可用；已修正文档与 runbook，明确只读核对无需停止 8080。`BLOCKED`（当时）：专用 login-path 尚未准备；该认证前置已由后续用户授权和 `T1-E06` 取代。`NOT_RUN`：读取/复制旧仓字面量密码、实际 schema 查询、数据库写入/初始化、进程停止、18081 启动、Provider 请求 |
| T1-E06 | T1.2 共享库历史只读诊断（已被 runtime fork 决定取代） | 本机 MySQL；仅 `SELECT`/`information_schema`；旧 8080 保持运行；未调用 Provider | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; dirty:三份预期文档更新` | 旧、新配置及运行 JAR目标四项脱敏比较；授权的同目标旧项目凭据仅经临时子进程环境使用；`SELECT DATABASE(), @@port, VERSION()`；`information_schema` 结构统计 | `HISTORICAL`：只说明当时共享 `ai_video` 的结构，不再是 VideoOps 目标或可继续施工的依据。未写库、凭据已清理；禁止在该库继续迁移或初始化 |
| T1-E07 | Runtime fork 决策与独立依赖清单 | 当前仓库、本机服务与源码；只读；未调用 Provider | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; dirty:预期基线变更` | 配置键/调用路径静态追踪；MySQL 候选目录存在性；无口令只读登录；Redis `CONFIG GET databases`/各 DB `DBSIZE`；端口监听；OSS key 构造链 | `PASS`：18081 空闲；Redis 16 库，DB0=2、DB1～15=0，DB15保留给 IT，因此选 DB14；Redisson/Sa-Token 可双前缀隔离；候选本地目录均不存在且被 Git 忽略。`FAIL`：现有 dev 配置仍指向旧 DB0/空前缀，OSS 两条裸 key 绕过 prefix，本地秘密加载仅覆盖 JWT 类。`NOT_RUN`：权威 MySQL schema 冲突查询/建库/迁移/seed、旧业务数据读取或导入、OSS/Provider 调用、18081 启动、8080 停止 |
| T1-E08 | Runtime fork 的安全 namespace 落地与配置门禁 | 当前仓库、本机 Redis；未启动应用；未调用 Provider/OSS | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; dirty:独立基线配置、测试、脚本与文档` | `SET NX videoops-agent:dev:__namespace_marker__`；DB0/DB14 脱敏计数；Git ignored 目录与端口复查；`Invoke-Pester scripts/tests/start-local-user-api.Tests.ps1 -EnableExit`；两个目标 Maven 配置测试（显式 `maven.test.skip=false`）；`scripts/validate-development-standards.ps1`；`git diff --check` | `PASS`：DB14=1、owned=1、foreign=0、项目前缀频道=0，DB0=2 未变；5 个独立目录存在且被 Git 忽略；8080 仍监听、18081 未监听；dev profile 与 launcher 锁定 loopback:18081、新库、本机 Redis DB14/双前缀/独立 clientName、独立目录并关闭 Timeline/Whisper/RunningHub/OSS；旧 `AI_VIDEO_OSS_CONFIG` import 已移除，附加配置限于项目 `.local/videoops-agent`，三项本地秘密使用 dev 实际消费的项目变量且进程后恢复；Pester 6/6、用户端 2/2、管理端 3/3、开发规范、diff check 均通过。`FAIL`：T1.2A 尚未完成，OSS initializer 与两条裸 key 仍不满足隔离。`NOT_RUN`：MySQL schema/账号/迁移/seed、18081 启动、旧业务数据、OSS/Provider/付费生成、8080 停止 |
| T1-E09 | T1.2A 当前黄金链的最小 OSS fail-closed 与 object namespace | 当前 dirty 源码；Java/PowerShell 本地边界测试；未连接 OSS/MySQL/Provider，未启动 18081 | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; dirty:T1.2A 瘦身实现与文档` | 用户端 reactor：`-Dtest=VideoOpsObjectKeyTest,AssetServiceImplTest,CreationAssetServiceImplTest,RenderOutputLifecycleServiceImplTest,AiVideoOssConfigurationInitializerTest,CreatorOssConfigurationInitializerTest,CreatorOssConfigurationConditionTest,LocalRedisConfigurationTest`；管理端 reactor：`-Dtest=VideoOpsObjectKeyTest,SystemOssConfigurationConditionTest,PlatformAppSessionInfrastructureConfigurationTest`；`Invoke-Pester scripts/tests/start-local-user-api.Tests.ps1 -PassThru`；`git diff --check` 与开发规范校验 | `PASS`：用户端 reactor 76/76、管理端 5/5、Pester 8/8。关闭态用户端 Creator/local initializer 与管理端启动 initializer 不装配；用户端开启路径只接受精确 `videoops-agent/dev`，项目 helper 拒绝空、旧、绝对与逃逸 key；人物/声音上传、数字人输出登记和 Timeline 成品使用限定 key。`DEFERRED`：RunningHub、管理端 OSS CRUD、通用 OSS/异常/全局日志治理。`NOT_RUN`：真实 OSS/SDK/bucket、MySQL schema/账号/迁移/seed、18081、Provider/付费生成；配置与 Mock 边界测试不等于应用已独立可运行 |
| T1-E10 | T1.2 独立 MySQL 空库 bootstrap、最小权限与旧状态反向对账 | 本机 MySQL 8.4.9、Redis；真实 DDL/DML 只作用于新 `videoops_agent_dev`；未启动应用、未连接 OSS/Provider | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; dirty:bootstrap 包、静态门禁、既有独立基线与文档` | `scripts/validate-videoops-database-bootstrap.ps1`；Pester bootstrap 反向测试；manifest `001..090 → 900` 逐步执行；`information_schema` schema/postcondition；`900` 同摘要重复执行；`videoops_agent` 新库正向与 `ai_video` 负向探针；共享库结构/关键计数和 Redis DB0/DB14 前后快照；`git diff --check`、开发规范 | `PASS`：静态 validator 输出 `VIDEOOPS_DATABASE_BOOTSTRAP_OK`，Pester 16/16；35 张 InnoDB 表、114 个实际索引、104 个 CHECK 且全部启用、额外 view/trigger/routine/event=0；`av_asset.file_id` 为 nullable signed BIGINT；T1.2 当时的纯合成 seed 为 29 行，重复执行仍为 29；T1.3 后当前冻结 seed 为 33 行，此处保留历史结果。`sys_oss_config` 及人物/声音/资产/任务/创作表当时为 0。运行账号只具新 schema 的 `SELECT/INSERT/UPDATE/DELETE` 且无其他授权/角色，读取新库通过，访问 `ai_video` 被 1142 拒绝。共享 `ai_video` 表数 53，结构/关键计数前后相同；Redis DB0=2、DB14=1/owned=1/foreign=0 前后相同；管理员密码只在授权子进程内使用，项目新凭据仅保存为 Git 忽略的当前用户 DPAPI 文件，敏感环境残留=0。恢复证据：首次 `080` 仅因 postcondition 列名误写失败，9 表/26 目标索引/57 CHECK 已完整落地后修正并通过；首次 `900` 因跨排序规则变量比较得到 1267 且事务完整回滚，改为二进制精确比较后强制 ROLLBACK 诊断、正式执行和幂等复跑均通过。`NOT_RUN`：18081、Java 后端、前端代理、真实 OSS、Provider、公司业务数据导入与最终 MP4 |
| T1-E11 | T1.5 无 Provider user-api 启动与真实 HTTP smoke | 本机 Java 21、MySQL、Redis 与 loopback HTTP；当前 dirty JAR；真实认证/数据库/Redis，未连接 OSS/Provider | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; status paths:35; jar sha256:DE73E8CA370F7110FA71BD82084D82FCC1A243221FDA53E4993E5DE909E8D45F` | launcher Pester；runbook 5 个专项测试类；user-api Maven package；Web `src/config.test.ts`；PRE/POST MySQL/Redis/端口快照；真实 health/login/me/quota/人物与声音分页/logout；监听/进程/连接归属；`git diff --check`、开发规范 | `PASS`（本机可接受）：Pester 9/9，后端 52/52，user-api 打包成功，代理配置 6/6；当前 JAR 仅监听 `127.0.0.1:18081`，健康、纯合成账号登录、精确 10 项权限、积分 1000、人物/声音合法空列表和 logout 通过，Provider 连接为 0。本次 OSS 显式为 false；launcher 默认 false，但保留负责人未来显式开启入口。新库业务/任务/资产/OSS 行数保持 0，认证副作用为 `app_login_log` 0→1、`app_security_audit` 0→1；运行只命中新库/DB14，旧库、DB0、旧 8080 均未变化。该证据形成后 launcher 曾发生 ACL 调整，当时仅补充启动/health；其历史剩余风险已由 `T1-E14` 当前 launcher 的真实 `/studio` 完整登录链闭合。进程已停止、18081 已释放。`NOT_RUN`：该次 Web 页面、真实人物/声音/OSS、Timeline/任务、所有 Provider、付费生成、最终 MP4及旧 8080 停止态 |
| T1-E12 | T1.3 JVM 内 OSS client 与真实资产写入前权限门禁 | 当前 dirty 源码与本机新库；Mock/装配测试 + 真实 MySQL 事务；未启动 18081，未连接 OSS/Provider | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; status paths:37; jar sha256:01238DB33A555E3940669B24080894D0999BC2B4B49DBA8FB9D8BC6BDC437E3C` | `AssetServiceImplTest` 12/12；`AiVideoOssConfigurationInitializerTest` 8/8；`CreatorOssConfigurationConditionTest` 2/2；user-api Maven package；补权前置与两次事务后只读计数 | `PASS`：local OSS override 直接构造/复用 JVM client，旧 Redis cache bridge 已删除；`AssetServiceImpl` 只使用注入的项目 client，OSS=false/缺 client 时明确失败；22/22 与 package 通过。`FAIL`：两项权限/绑定首次写入因无临时表权限退出，定向重试仍 exit 1；安全执行器未保存原始错误类别，不能虚构根因。两次均完整回滚，新权限=0、新绑定=0、原权限/绑定=10/10。`NOT_RUN`：真实凭据注入、18081、人物/声音 API、OSS 写入/读取、Provider、付费与视频生成 |
| T1-E13 | T1.3 真实人物与原声资产产品链 | 当前 dirty JAR、本机独立 MySQL/Redis、loopback HTTP 与真实项目 OSS namespace；未调用 Provider | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; status paths:37; jar sha256:01238DB33A555E3940669B24080894D0999BC2B4B49DBA8FB9D8BC6BDC437E3C` | bootstrap validator 与 Pester；同连接分阶段权限事务；项目 OSS/资产专项测试与 package；真实 login/me、人物 upload/create/list/access、声音 upload/list/access、匿名授权读取、logout；MySQL/Redis/端口前后对账 | `PASS`：bootstrap validator 输出 `VIDEOOPS_DATABASE_BOOTSTRAP_OK`，Pester 16/16；`900`/manifest 固定 12 项权限与 12 条角色映射，现场为 permission/binding/effective=`12/12/12`。专项测试 22/22、package 通过。真实 API 全部 HTTP 200，人物 owned + ready、声音 owned + origin/unparsed；`av_asset/av_portrait/av_voice=2/1/1`，两个 key 均在 `videoops-agent/dev/`、foreign=0，两次授权下载 SHA-256 与输入文件一致。认证审计 `app_login_log` 与 `app_security_audit` 各 1→2；`sys_oss_config=0`、DB14 回到 marker-only 且 OSS key=0、DB0=2；项目账号访问 `ai_video` 仍被 1142 拒绝，旧 8080 PID/启动时间不变，18081 已释放，39000/8189 连接为 0，敏感环境残留为 0。`NOT_RUN`：ComfyUI、IndexTTS2、RunningHub、DeepSeek、Timeline、付费生成和最终视频 |
| T1-E14 | T1.4 + T1.6 + T1.7 真实 `/studio` 黄金链与最终 MP4 | 当前 dirty JAR、真实 Web/18081、新库/DB14、项目 OSS 与公司 GPU Provider；仅本次单次生成获授权 | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; status paths:47; jar sha256:B6F17F9AA0AA85ED7E168FB79AA11B87F473A3F523FF04B1F5CC98747F1CD653; launcher sha256:AB9C74826BBEDA5EC46FAE317270BF8239AA2C0F3AAB443B2C9FEEE9D4614E91` | 真实浏览器 `/studio` 登录/素材选择/固定文案/voice/video/project/render/下载；任务与资产只读对账；ffprobe、音量检测和 3 处抽帧；Provider 9/9、存储 20/20、任务详情 19/19、前端 10/10、launcher Pester 12/12；user-api package；Redis/端口/进程收工快照；`git diff --check`、开发规范 | `PASS`：页面无 Mock 且不经过旧 8080；voice/video/render 各只 POST 一次并成功，任务、项目、三条 creation asset 与 output asset 因果链一致，所有存储 key 均在 `videoops-agent/dev/`。实际工作流 UUID/JSON SHA 与模型节点已冻结；Comfy 上传子目录为项目 namespace。下载文件 `videoops-t1-golden-2088539141703839746.mp4` 为 9,544,664 bytes，SHA-256 `67D6819F5A2F693516A6C239DF55EB3D9223E3491B9C181CF84EB25A4842BF09`，25.8 秒、1080×1920、30 fps、H.264 + AAC，音轨非静音，3 处抽帧均见烧录中文字幕。DB14 收工回到 marker-only、DB0=2；项目账号访问 `ai_video` 仍被 1044 拒绝；旧 8080 PID/启动时间/命令摘要不变；18081/8002 已释放，本机 39000/8189 未启动，敏感环境残留为 0。`NOT_RUN`：RunningHub、DeepSeek、T1.8 正式质量打分/0.25 秒一致性、T1.9 独立收口；未提交代码 |
| T1-E15 | T1.8 字幕可读性修复与成品一致性验收 | 当前 dirty JAR、本机 Timeline/FFmpeg、新库/DB14 与项目 OSS；复用既有 voice/video，未调用 Provider | `branch:main; commit:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; git status --porcelain=v1 -uall:60 paths（42 tracked + 18 untracked）` | project `2088580138567495681` → render task `2088580140291354626` → output asset `2088580141574811650`；下载 `videoops-t1-final-readable-2088580140291354626.mp4`；SHA-256、ffprobe、完整解码、black/freeze/silence 检测、8 段字幕与人工观感；Provider/数据库/Redis/端口前后对账 | `PASS`：新文件 5,244,591 bytes，SHA-256 `70AF66B5D57A43B3626DF1FE3C1CC85417010EC600C04050F3A93BA77FFEE0B7`；25.8 秒、1080×1920、30 fps、H.264 + AAC，音视频 start/end delta 均为 0，完整解码 exit 0，black/freeze/silence 均为 0。8 段字幕统一黑色 3 px 描边，人工观感较旧成品改善；底片与声音身份保持 T1.7 因果链，Provider job 总数仍为 2、无新 Provider 调用。DB0=2、DB14=marker-only、`sys_oss_config=0`，旧 8080 PID 不变，18081/39000/8189 均已释放。`NOT_RUN`：IndexTTS2、ComfyUI、RunningHub、DeepSeek 再调用 |
| T1-E16 | T1.9 当前源码总门禁与 checkpoint | 当前 checkpoint 源码；本地测试、构建、静态门禁、运行状态对账与独立复核；未调用 Provider | `branch:main; checkpoint:本提交; base:e999b1f2ce1f3d62168c3668de22b0fa6f8f9159; input status paths:60; jar sha256:8174D10B91DF06ADA6A14661ACDA268756BCD08A200D50DFFFFDBAA621C77289` | 后端匹配测试与 user-api package；前端匹配测试、lint、tsc、build；launcher/bootstrap Pester；bootstrap validator；开发规范；staged diff/秘密/媒体门禁；新库/Redis/端口最终对账；未实施者独立审查 | `PASS`：后端 17 类 155/155，user-api package 成功；前端 8 文件 45/45，lint/tsc/build 全绿；launcher Pester 12/12、bootstrap Pester 16/16，bootstrap validator、开发规范和 staged diff check 全绿。59 个 ACMR 路径加 1 个删除全部暂存，二进制、`.runtime/.local`、媒体与高信号秘密命中均为 0；冗余日志/缓存已删除，仅在 ignored output 保留最终 MP4 与联系表。最终新库为 35 表、2 个成功 Provider job、2 个 ready project、2 个成功 render task/输出，全部 storage key 在项目 namespace，`sys_oss_config=0`；DB0=2、DB14=marker-only/foreign=0/OSS-like=0，运行账号访问 `ai_video` 被 1142 拒绝，旧 8080 PID/启动时间/命令摘要不变，18081/39000/8189 均未监听。未实施者复核为 `Accept for local/pilot checkpoint`，P0/P1=0；生产发布仍 `NOT_RUN` |
| T2-E01 | T2 版本化契约、AgentRun 恢复/fencing 与迁移边界 | 当时的 T2 dirty 源码；Java 单元/编译与 SQL 静态门禁；真实 MySQL IT 当时尚未执行 | `branch:main; base checkpoint:1fe6a92a23e6d8b4f8aa12552f09c1097498ba9e; status paths:19（18 个 T2 + 根 AGENTS）；migration sha256:67D8C51C5A7A7C873A32A33A76CB6122325BFA83D0B4ADB6C78BDD228EC8B5F0` | `AgentRunServiceImplTest`；`AgentRunPersistenceIT` testCompile；bootstrap validator/Pester；开发规范；`git diff --check`；未实施者 fresh review；本机管理员只读能力与对象存在性门禁 | `HISTORICAL PASS/BLOCKED`：三张控制面表、manifest 顺序 `001..100→900`、38 表/126 索引/129 CHECK 静态合同通过；Service 单元 9/9、IT testCompile 与 reactor 编译通过；任务/候选资产事实、`DATETIME(6)`、CHECK UNKNOWN 及 waiting 外部任务失败/取消死路已收口。该时点因秘密处置和既有测试对象归属未确认而暂停，尚未执行 live IT；负责人后续接受风险并确认测试 schema 来源，阻塞已由 `T2-E02` 闭合。`NOT_RUN`：Provider、HTTP/UI、T3+ |
| T2-E02 | T2 真实 MySQL 恢复、迟到结果与最小权限验收 | 当前 T2 checkpoint 源码；本机 MySQL `ai_video_test`；Redis 仅只读前后摘要；未访问 Provider/HTTP/UI | `branch:main; checkpoint:本提交; base:1fe6a92a23e6d8b4f8aa12552f09c1097498ba9e; input status paths:19; migration sha256:67D8C51C5A7A7C873A32A33A76CB6122325BFA83D0B4ADB6C78BDD228EC8B5F0` | `AgentRunPersistenceIT` Failsafe；`AgentRunServiceImplTest`；bootstrap validator/Pester；开发规范；diff/秘密门禁；MySQL 表残留、账号授权与双开发库负向探针；Redis DB0/DB14 只读摘要 | `PASS`：当前源码 Failsafe 报告新鲜 3/3，failure/error/skipped 均 0；随机六表残留 0，既有三张测试表原样保留。`videoops_agent_t2_it` 仅获 `ai_video_test.*` 的 `CREATE/DROP/SELECT/INSERT/UPDATE/DELETE`，无全局、对象、角色或 Grant Option，访问 `ai_video` 与 `videoops_agent_dev` 均被 1044 拒绝。旧库结构仍为 53 表，独立开发库仍为 35 表；Redis DB0/DB14 的 `DBSIZE` 前后为 2/1，IT 源码未创建 Redis client。单元 9/9、validator、Pester 16/16、开发规范与 diff/秘密检查全绿；敏感环境残留 0。`NOT_RUN`：Provider、HTTP/UI、T3+ |
| T3-E01 | T3 类型化黄金链工具、幂等安全回放与成品对账 | 当前 T3 checkpoint 源码；真实 Spring/MyBatis 只读开发库边界 + 本地已授权 T1 成品；Provider/真实 OSS 写入关闭 | `branch:main; checkpoint:本提交; base:a03f5056b0ed2ca0864eb96239da90592bdd88bf; input status paths:13; jar sha256:8154A424BEBECE862DBC2EEEDE3042DD1C05928C8C79A816A68195F0B88B25E9` | `AiTaskTransactionServiceImplTest`、`AgentToolServiceImplTest`、`TimelineTaskApplicationServiceTest`；user-api package；真实工具只读回放；本地 MP4 SHA-256、ffprobe 与完整解码；开发规范、staged diff/秘密/媒体门禁 | `PASS`（本机/试运行 checkpoint）：聚焦测试 32/32，user-api package 成功；8 把工具使用显式 switch 和强类型参数，权限/principal/owner/多余参数正反用例通过。真实工具入口读取终态 voice `2088530227838955521`、video `2088530778710454273`，以原 high render 身份对 project `2088539060225290242` / task `2088539141703839746` 安全回放两次均返回同一根任务，异 revision 为 46609、跨 owner 拒绝，任务/execution/attempt/project/asset/job 摘要前后相同。最终可读 task `2088580140291354626` / asset `2088580141574811650` 为当前 owner 的 ready `timeline_render_output`；本地 MP4 为 5,244,591 bytes、SHA-256 `70AF66B5D57A43B3626DF1FE3C1CC85417010EC600C04050F3A93BA77FFEE0B7`，25.8 秒、1080×1920、30 fps、H.264 + AAC，完整解码 exit 0。T1.8 原任务为 `standard`，不冒充固定 `high` 的 render replay；因此用 T1.7 high task证明回放、T1.8 final task证明最终成品。18081/39000/8189 均未监听，临时夹具与敏感环境已清理。`NOT_RUN`：新 voice/video/render 提交、真实 Provider、真实 OSS 下载或写入、HTTP/UI、T2 AgentRun 接入与 T4 |
| T3-E02 | T3 独立审查后的历史成品关联与失败事实修复 | 当前窄修复 checkpoint 源码；本地单元/Mapper 契约与 user-api package；未连接 DB/Redis/Provider/OSS | `branch:main; checkpoint:本提交; base:1768a6ea0c9cb5080079a0ecd6039d23619ddb00; input status paths:13; jar sha256:E32E1065422421F33F47B8CE877146E2A01193F0BF01996FE71F165B3046A7EA` | 旧实现反例；`AiTaskTransactionServiceImplTest`、`CreationAssetServiceImplTest`、`TimelineServiceBoundaryTest`、`DigitalHumanGenerationServiceImplTest`、`AgentToolServiceImplTest`、`TimelineTaskApplicationServiceTest`；user-api package；开发规范、diff 与 staged 秘密/媒体门禁 | `PASS`（merge checkpoint）：旧实现对“task A 后项目 latest 已为 B”反例稳定报 46704；修复后 Mapper 以单条 JOIN 同时冻结 task/asset owner、task success/resultAsset、asset source/ready/video/origin，工具查询 A 稳定返回 asset A 且不读 latest。render、voice、video 的 failed 结果返回持久化 stable code + safe message，缺任一事实则 46704；测试夹具 taskType 已纠正为 `timeline_render`。六类 82/82、failure/error/skipped=0，package 成功；无新工具、表、Controller、UI 或外部调用。`NOT_RUN`：真实 MySQL 执行新增关联 SELECT、HTTP/UI、Provider、OSS、FFmpeg、T4 |
| T4-E01 | T4 确定性黄金链、跨实例恢复、预算/取消与迟到结果 | 当前 T4 checkpoint 源码；本机 MySQL `ai_video_test` 的真实事务/CAS + 生产编排 Service + 幂等 counting fake 工具边界；未连接 Provider/OSS/Redis/HTTP | `branch:main; checkpoint:本提交; base:753666310745bc55657add2d09467ecae0a589a5; input status paths:17; migration sha256:67E074542ED120E6FF47A15A610A3254809E1BA009105772697E3B6DE1D367B2; jar sha256:60BFA15AE69B2283CD515E21159885166C418386DBAD6C12AF425506CF3378ED` | `AgentRunPersistenceIT` Failsafe；`AgentRunServiceImplTest`、`AgentRunOrchestrationServiceImplTest`、`AgentToolServiceImplTest`；clean user-api package 与 fat JAR entry 核验；bootstrap validator/Pester；开发规范；diff/秘密/媒体门禁；随机表与账号隔离后置检查 | `PASS`（本机/试运行 checkpoint）：真实 MySQL 5/5，failure/error/skipped 均 0；跨 fresh Spring context 从 voice waiting 恢复到 video、render 与 owned ready output，voice/video/render 三类提交各只接受 1 次，恢复始终查询/换绑同一持久任务。跨 owner cancel 被拒，owner cancel 后迟到 success 在 fresh context 工具调用与状态变化均为 0；缺输入、权限、期限、提交/恢复、render 重试预算与稳定失败转人工由聚焦单元 32/32 覆盖。随机 T4 七表残留 0，既有三张共享测试表仍为 3，测试账号访问 `ai_video`/`videoops_agent_dev` 均被拒。clean package 成功，fat JAR 内 T4 编排入口精确 1 个；bootstrap validator、Pester 16/16、开发规范与 diff check 全绿。`NOT_RUN`：迁移真实 `videoops_agent_dev`、真实 Provider/OSS/Redis、HTTP/UI、T5；counting fake 不冒充 Provider 实跑 |
| T4-E02 | T4 恢复次数语义与 Provider 接受后崩溃窗窄修复 | 当前窄修复 checkpoint 源码；本机 MySQL `ai_video_test` + 真实类型化工具、资源生成/生成 Service；最外层声音 Provider adapter 为 counting fake；未连接真实 Provider/OSS/Redis/HTTP | `branch:main; checkpoint:本提交; base:bc7ed68030729e1cba7a4315a5fb1ad9e483b8e0; input status paths:6; jar sha256:BFB02EF2EC8CA924F22E4F39E4D300A5A8A3DF292306642C43F6C9697DB4D56B` | `AgentRunOrchestrationServiceImplTest`；core `AgentRunPersistenceIT` 与 user `AgentRunCrashWindowIT` Failsafe；`AgentRunServiceImplTest`、`AgentToolServiceImplTest`；user-api package 与 fat JAR/current class SHA 对账；开发规范、diff/staged 秘密与媒体门禁；独立监督审查 | `PASS`（本机/试运行 merge checkpoint）：generation 0 初领、generation 1 首次 fresh 查询、generation 2 才耗尽，submit/status 各 1 且 taskId 不变；聚焦单元 33/33。core MySQL IT 5/5、user 崩溃窗 IT 1/1，failure/error/skipped 均为 0；故障发生于真实工具与生成链返回后、waiting CAS 前，fresh instance 以同一幂等键恢复原 job，Provider adapter=1、AgentRun/生成根任务各 1。随机七表残留 0，既有三张共享测试表仍为 3。当前 user-api package 成功，fat JAR 中编排类 SHA-256 与当前 `target/classes` 精确一致；独立审查为 `Accept for local/pilot merge checkpoint`，P0/P1=0。`NOT_RUN`：user 模块全量 Surefire（既有无关失败）、真实 Provider/OSS/Redis、HTTP/UI、真实 `videoops_agent_dev` 迁移与 T5；不冒充生产 ship-ready |
| T5-E01 | T5 当前成品三层逐项质量验收 | 当前 T5 checkpoint 源码；T1 已授权下载的 ignored MP4、本机真实 FFprobe/FFmpeg；任务/项目/不可变版本事实只读准备后经生产质量入口验证；未连接 Provider/OSS/Redis/HTTP | `branch:main; checkpoint:本提交; base:a66c7cfbbb0de4c75a75c9cfc82deee442d84ccc; input status paths:25; jar sha256:E3C3EDB4DAF7C8ECF05B7AE2798AEB1E03ADF222D41139440178CF3B1ADE1115` | `TimelineOutputQualityServiceImplTest`、`CreationAssetServiceImplTest`、`TimelineDtoContractTest`、`TimelineServiceBoundaryTest`、`FfmpegTimelineMediaRenderServiceTest`、`AgentToolServiceImplTest`、`AgentRunOrchestrationServiceImplTest`；`TimelineOutputQualityLocalIT`；user-api package 与嵌套 T5 class 对账；开发规范、diff/staged 秘密与媒体门禁 | `PASS`（本机/试运行 checkpoint）：聚焦单元 77/77；真实 LocalIT 1/1，failure/error/skipped 均为 0。任务 `2088580140291354626`、output asset `2088580141574811650` 与不可变 render input `2088580140345880578` 被 owner-scoped 绑定；MySQL JSON 解析为 DTO 后使用同一 `JsonMapper` canonicalize 再校验 content hash，物理格式变化通过、语义变化失败。成品 5,244,591 bytes，SHA-256 `70AF66B5D57A43B3626DF1FE3C1CC85417010EC600C04050F3A93BA77FFEE0B7`；真实 FFprobe 与完整解码得到 25.8 秒、1080×1920、30 fps、H.264 + AAC。250/251 ms 由质量层基于 probe 事实判定；renderer 只保留来源、大小与 SHA fail-closed。固定 16 项结果为 9 `PASS`、7 `REVIEW`、0 `FAIL`，三层为 5/6/5，无总分；must/prohibited 未配置与 5 项感知事实不冒充 PASS。首次 LocalIT 仅在 FFmpeg 启动前拒绝 WinGet 链接，改用同一安装的真实可执行路径后 fresh 通过，旧红报告不作证据。当前 fat JAR 已核对嵌入 T5 质量类。`NOT_RUN`：真实 OSS/Provider、重新生成/付费、OCR/ASR/人脸/口型 evaluator、HTTP/UI、T6/T7 |
| T5-E02 | T5 环境/损坏分类、语义标点与真实成品关联窄修复 | 当前 T5 窄修复源码；本机 MySQL `ai_video_test` 的生产 Mapper/Service；最外层为计数本地存储 adapter；T1 已授权下载的 ignored MP4 与本机真实 FFprobe/FFmpeg；未连接真实 OSS/Provider/Redis/HTTP/UI | `branch:main; checkpoint:本提交; base:7149489e65d9c7f3806eb2e5af41ef97f5ef5e8e; input status paths:14; jar bytes:188974617; jar sha256:DD624F8D4F0E54CB0C641D18D53AB4DFF52BB4BEA5F9B958AF6187C1670CA2A6` | 11 类聚焦测试；临时 `TimelineOutputQualityPersistenceIT` Failsafe 报告；当前 T1 MP4 与截断同字节流；user-api package 与 fat JAR/module/class 字节对账；开发规范、diff/staged 秘密与媒体门禁 | `PASS`（本机/试运行 checkpoint）：聚焦测试 98/98，failure/error/skipped 均为 0；其中原 77 项全部保留。临时真实边界 Failsafe 1/1、0 skip：生产 `CreationAssetMapper/Service` 在 `ai_video_test` 验证精确 owner/task/resultAsset/sourceRef 正例，跨 owner、错 task、错 asset 与错 sourceRef 均在本地存储读取前拒绝，随机表精确清理为 0；最外层本地 adapter 不冒充 OSS。当前 T1 MP4 为 5,244,591 bytes、SHA-256 `70AF66B5D57A43B3626DF1FE3C1CC85417010EC600C04050F3A93BA77FFEE0B7`，真实 FFprobe 与完整解码通过；截断同一字节流稳定判定媒体输入损坏。renderer 关闭、超时及存储不可达统一输出 5 项低置信 `REVIEW`，只有确证输入损坏才把 `media.playable` 判为高置信 `FAIL`。保存规范化、trusted/Whisper 对齐、ASS 与质量判定共用 `SubtitleDisplayNormalizer`：仅明确句读白名单可省略，`/`、`-`、`.`、`:` 默认保留，`3.14→314`、`12:30→1230`、`8/16→816` 与 `A-12→A12` 均拒绝，普通句读/空白仍可等价。旧 257 行 LocalIT 已删除；本次 248 行临时真实边界源码在验收后删除，未进入 checkpoint。当前 package 成功，fat JAR 内 core/infra 模块及四个关键 class 与当前构建输出字节一致。`NOT_RUN`：真实 OSS/Provider/Redis、HTTP/UI、重新生成或付费、T6/T7；P2 JSON 等值词法的更宽边界未扩张，仍以 DTO canonicalize 合同为准 |
| T6-E01 | T6 有界依赖返工、三类批准与恢复控制面 | 当前 T6 checkpoint 源码；本机 MySQL `ai_video_test` 的真实事务/CAS；外层幂等 counting tool 与最外层声音 Provider adapter；未连接真实 Provider/OSS/Redis/HTTP/UI | `branch:main; checkpoint:本提交; base:ec8f288dca73d70ea50c16957e88fbbd6bce00b2; input status paths:26; migration sha256:66642F11397EE38968A8DEAC8B90118E3EB575585D41B23C823685AA479273D2; jar bytes:189025356; jar sha256:122870BBD03430578C840FEAEB16D2A9B7C332DD5B8CABF672A24451EE92F379` | 6 类聚焦单元；`AgentRunPersistenceIT`、`CreationTimelineIsolationIT`、`AgentRunCrashWindowIT` Failsafe；bootstrap validator/Pester；user-api clean package 与嵌套 core SHA 对账；开发规范、diff/staged 秘密与媒体门禁；独立 own-loop 复审 | `PASS`（本机/试运行 checkpoint）：聚焦单元 90/90；真实 MySQL 分别为 6/6、3/3、1/1，failure/error/skipped 均为 0。质量结果在持久边界重新解析固定 16 项并绑定 owner、render task、ready result asset 与 SHA，服务端复算返工/批准决定；`quality_repair_count` 与技术 retry/lease generation 独立且最多 2。字幕高置信失败只创建同一 video job 的新项目与 render，媒体失败只重 render；fresh context 对 project/render 各重入 2 次但唯一幂等身份各只接受 1 次，原 voice/video 两行与父子 ID 不变。initial/conditional/final 批准均由 owner/type/row/contract/approval revision、canonical personal workspace 与冻结 Brief 所需权限约束；MySQL 还直接拒绝 conditional/final 的 NULL evaluation 与终态批准的 NULL decider。Provider 接受后、waiting CAS 前崩溃由真实工具与生成 Service 证明 fresh replay 仍为同一 job、最外 adapter submit=1。条件批准按冻结合同进入 `waiting_input`，需新 Brief/Profile 与新 run，不冒充同 run 自动继续。随机表清理、旧三张共享测试表与双开发库拒绝已在本轮后置门禁通过；敏感环境残留 0。当前 clean package 的嵌套 core SHA 与当前 core artifact 精确一致；独立复审 P0/P1=0。`NOT_RUN`：同一测试进程内真实 Timeline application Service + AgentRun 的 monolithic crash E2E、迁移真实 `videoops_agent_dev`、真实 Provider/OSS/Redis、HTTP/UI、T7；组合证据不冒充生产 ship-ready |
| T7-E01 | T7 Agent HTTP/Trace、默认页面与无外部调用运行烟测 | 当前 T7 代码就绪 checkpoint 源码；本机 Java 21、loopback 18081、生产 Web preview 8002；真实开发库/DB14 连接，仅认证前置；Provider/OSS/Timeline 均关闭 | `branch:main; checkpoint:本提交; base/origin-main:45926f642c3d0ca54b22d6fac6044dd554fb7425; input status paths:33; jar bytes:189080091; jar sha256:FF3630A8A4335305EBF32CA4D55C5CE2B1362A546675A2BFDFACC00CA50253FD` | core/user 五类聚焦测试；前端四类聚焦测试、tsc/build；user-api package；当前 JAR 18081 启动；生产 preview `/agent`；浏览器 DOM/console；端口、临时文件和敏感环境后置清理 | `CODE READY / NEEDS_RUNTIME_MIGRATION / E2E NOT_RUN`：后端 46/46、前端 39/39，failure/error/skipped 均为 0；tsc、生产 build（70 assets）与 user-api package 通过。当前 JAR 在 Provider/OSS/Timeline 全关闭时独占 `127.0.0.1:18081`；生产 preview 的 `/agent` 为 HTTP 200，浏览器由真实认证门禁重定向到 `/user/login?redirect=/agent`，console error/warn 为 0。Web 先停、18081 后停，两个端口均释放，旧 8080 PID `34172` 保持不变；一次性启动/清理文件、6 份 ignored 日志与敏感环境均清零。`E2E NOT_RUN`：该证据时点 `videoops_agent_dev` 尚未应用 `100/110/120` 且无当前可用管理员身份；本轮没有登录、创建 AgentRun、执行四场景或调用 Provider/OSS。当前仓库已公开、`origin/main` 已前进到 `3740c5e...`，以 `T7-E04` 的实时远端事实为准；本 checkpoint 不 push。 |
| T7-E02 | T7 开发库 AgentRun 迁移与 MySQL 8.4 postcondition 兼容 | 负责人授权的本机管理会话负责真实 DDL；checkpoint 仅修改 120 自检与静态边界测试，Codex 未读取凭据或连接数据库 | `branch:main; checkpoint:3740c5e38cc6f19f466c34b16fa1cbf5561966a7; base:1a58f224837261b5cfee111982c2d7636c01ac51; migration sha256:F119D4F5ABDA690285B2C7600DC46BFA797DA5571BD5887951E6B87F2976AF90` | 负责人报告的 100/110/120 执行结果与结构计数；MySQL 8.4 `CHECK_CLAUSE` 反引号 counterexample；bootstrap validator/Pester；开发规范、diff 与 staged 秘密/媒体门禁 | `PASS`（本机/比赛 runtime checkpoint）：负责人仅重跑当前 120，迁移自身 validator 成功；AgentRun 表=5，quality columns/tables/indexes/checks=`3/2/5/12`，normalized null checks=2。100/110 未重跑、900 未执行，旧 `ai_video` 表数前后均为 53。源码仍精确要求 `evaluation_id IS NOT NULL` 与 `decided_by IS NOT NULL`，未放宽业务约束；Pester 17/17、bootstrap validator、开发规范与静态门禁全绿。真实 `/agent` 全链转入 `E2E IN_PROGRESS`。 |
| T7-E03 | T7 真实 AgentRun、批准、重启与首个运行根因 | 当前 `3740c5e...` Java/Web 源码与本轮 launcher；独立 18081/8002；真实 `videoops_agent_dev`、DB14、项目 OSS namespace、当前 creator；已授权原声/人物；真实 IndexTTS2 一次调用。未调用 ComfyUI/RunningHub/DeepSeek，未启动第二次生成 | `branch:main; checkpoint:本提交; base:3740c5e38cc6f19f466c34b16fa1cbf5561966a7; run:2089047561322209281; voice job:2089048158523990017; voice resource:2089047382502252546; portrait resource:2089047375984304129; jar bytes:189080091; jar sha256:FF3630A8A4335305EBF32CA4D55C5CE2B1362A546675A2BFDFACC00CA50253FD` | 真实 `/api/agent` create/detail/advance/approval；stale revision；进程 PID/端口与同 task 重启；owned job 查询；浏览器未登录认证门禁；后端 5 类聚焦、前端 4 类、launcher Pester、package/tsc/build、开发规范、公开快照扫描、diff/端口/临时文件清理 | `PARTIAL PASS / E2E BLOCKED`：固定中文脚本与 owned 资源创建同一 AgentRun；initial approval 成功，过期 rowVersion 以 46705 拒绝且 run 不变。唯一声音 job 进入 `running/voice_synthesizing`；重启 18081 后仍查询同一 job/idempotency，未创建第二 job、未盲目重提，最终 job 与 run 均以 `GENERATION_TIMEOUT`/安全摘要转人工，Trace=3、repair=0、无 video/render/final asset。源码确认 IndexTTS2 是同步 300 秒请求而 voice 总门禁为 10 分钟，且 voice job 没有 provider job id；重启窗口的外部结果不可恢复是首个产品根因。后端 46/46、前端 39/39、launcher 13/13、scanner Pester 2/2，failure/error/skipped=0；tsc、70-asset build、package、开发规范与公开快照扫描通过。Web→18081 顺序停止，8002/18081 释放，旧 8001 PID `32476` 与 8080 PID `34172` 未变；14 个一次性运行文件删除，敏感父环境残留为 0。`NOT_RUN/BLOCKED`：成功 MP4、局部返工、final approval、跨 owner（第二 app 身份不可用）、已登录浏览器；DB0 按隔离规则不读取，未再次执行 100/110/120/900。 |
| T7-E04 | T7 owner-scoped 成功任务复用入口与当前运行阻塞 | 当前复用入口源码、单元/契约测试、生产 Web build 与 user-api package；未连接 Provider/OSS/Redis，未写数据库或启动服务 | `branch:main; checkpoint:本提交; base:4f30a46a62d6646550117ced89283c5f544b89e1; input status paths:19; jar bytes:189085165; jar sha256:690001A84162625DCFA092A61428B58E2DC62A551961FB45A18575A2FEF03CA7; origin/main:3740c5e38cc6f19f466c34b16fa1cbf5561966a7` | 后端 `DigitalHumanGenerationServiceImplTest`、`AgentRunOrchestrationServiceImplTest`、`AgentToolServiceImplTest`、`AgentRunApplicationServiceImplTest`、`AgentRunControllerTest`；前端 agent API/page + app/routes；Biome、tsc、Ant Design lint、生产 build；user-api package；端口与敏感环境后置核对 | `CODE READY / RUNTIME E2E NOT_RUN`：`/agent` 创建合同现在显式支持 `new/voice_job/video_job/project/render_task`，默认 `new` 不变；voice/video 由服务端 owner-scoped DB-only 读取并冻结 64 位 input hash，复用时再次校验任务 ID、类型、成功/输出、voice confirmed 或 video parent，错 owner/类型/状态/hash 在新 Provider/项目/任务副作用前拒绝。project/render 起点仍由既有 owner-scoped 工具边界校验，成功 render 必须有 result asset；冻结 startAt 决定后续权限与 Provider 预算，浏览器不提交 owner/hash/权限。后端 73/73、前端 45/45，failure/error/skipped=0；Biome 无 error、tsc、Ant Design lint、70-asset production build 与 package 均通过。`NOT_RUN/BLOCKED`：现有 DPAPI 运行账号认证失败，旧 dev 配置身份启动也被 MySQL 拒绝；一次受控官方 YAML/connector 读取方法按同根因失败两次后已停止，未输出秘密。因而未实时读取 T1 任务行、未启动 18081、未登录或调用本轮复用入口，T1 历史 ID 仅作冻结历史证据，不冒充当前 E2E；Provider/OSS/Redis/DB 写入均为 0，18081/8002 已释放，敏感进程环境名为 0。GitHub 仓库已公开，远端操作由监督线程负责，本 checkpoint 未 push。 |
| T7-E05 | T7 project/render 创建前零副作用预检与运行身份复核 | 当前窄修复源码、聚焦单元和 user-api package；两条 Git 忽略一次性凭据辅助均在运行后删除；未连接 MySQL/Provider/OSS/Redis，未启动 18081 | `branch:main; checkpoint:本提交; base/origin-main:578b2567a8035244bdbd57ee0ec0f3be1ffa50fb; input status paths:3; jar bytes:189086353; jar sha256:E079816C67AD8798323E985E537B1C50D1D60F8C3729924BE74F7179471D259C` | `AgentRunServiceImplTest`、`AgentRunApplicationServiceImplTest`；user-api package；开发规范、公开快照扫描、diff/staged 秘密与媒体门禁；载体时间和端口后置检查 | `CODE READY / RUNTIME E2E NOT_RUN`：project 复用在首个合同写入前读取 owned project 并核 projectId、`digital_human_job` source、sourceId、editing/ready、精确 draft revision、timeline schema 与画布事实；render 复用在首个合同写入前调用既有 `get_timeline_render_status`，要求 owner-scoped `timeline_render` success、精确 project/revision/resultAsset 与 owned ready `timeline_render_output`。错误 project revision 与非 success render 均以 46704 拒绝且 Brief/Profile/Run 写入为 0；聚焦 26/26、failure/error/skipped=0，package 成功。运行账号恢复未到 MySQL：Spring loader `STAGE=11`，标准 YAML parser `STAGE=12`；DPAPI carrier 字节和时间未变，临时辅助残留 0。`NOT_RUN`：开发库 T1 事实对账、18081 登录、真实 `/agent` 复用、质量/批准/Trace/下载、四场景、Provider/OSS/Redis；README 与远端均未修改。 |

## 下一条准确动作

`T7 RUNTIME E2E BLOCKED`：需要一个可解密且字段形状明确的本机 MySQL 管理载体，或由负责人在受控会话中把 localhost `videoops_agent` 重置为仅具 `videoops_agent_dev.*` 的 SELECT/INSERT/UPDATE/DELETE 并同步现有 CurrentUser DPAPI Database 载体。恢复后先只读核同 owner 的 T1 voice/video/project/render 事实，再从最靠后的合法起点调用当前 `/agent` 复用入口。不得重试本轮两条已失败解析路径，不得用新 Provider 提交掩盖恢复缺口，不得再执行 100/110/120/900；本线程不改 README、不 push、不改远端。

## 开工与收工协议

- 开工：读取本文件及其点名的当前辅助 runbook；确认绝对路径、分支、HEAD 和工作区。只有调整路线、范围或依赖时才读 `PLAN.md`。发现未记录改动时先查来源，不得覆盖。
- 进行中：只在步骤通过、状态变化、出现阻塞或准备交接时更新本文件；运行日志和大型产物留在授权存储，只在此记录脱敏摘要、安全引用与哈希。
- 收工：检查 diff，运行 runbook 规定的验证，记录失败和未运行项。AI 总结、静态阅读、Mock 或“看起来正常”不能单独支撑 `DONE`。
