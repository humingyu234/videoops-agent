# 当前施工状态

本文件是“现在做到哪里”的唯一实时来源。全局阶段含义见 `docs/PLAN.md`，阶段边界见当前任务卡。这里只在子步骤通过、状态变化、出现阻塞或准备交接时更新，不写逐分钟流水账。

## 当前快照

| 字段 | 值 |
| --- | --- |
| 当前阶段 | T1：人工黄金链 |
| 当前任务卡 | `docs/tasks/T1-golden-path.md` |
| 当前详细计划 | `docs/superpowers/plans/2026-08-14-videoops-agent-t1-golden-path.md` |
| 状态 | `NOT_STARTED` |
| 风险等级 | `RED`：真实 Provider、文件/资产、异步任务、幂等和可能的付费调用 |
| 开工基线 | `cab31a0d16fa3f2a1a2f65d9fbff9fd8d117905a` |
| 最近验证源码 | 尚无 T1 运行证据 |
| 工作区 | `dirty`：仅本轮文档与规范验证脚本变更；提交前不得进入 T1 运行 |
| 最后更新 | 2026-08-14（Asia/Shanghai） |
| 更新者 | Codex |

## 子步骤

| 子步骤 | 状态 | 当前证据 | 下一准确动作 |
| --- | --- | --- | --- |
| T1.1 安全环境清点 | `NOT_STARTED` | 只读预查发现 JDK 21 未进入 PATH、8080 被未知进程占用、39000/8189 关闭 | 配置 JDK 21；保留未知 8080 进程并计划使用 18080 |
| T1.2 数据库与迁移核对 | `NOT_STARTED` | 3306/6379 可连接，但目标库、迁移和认证未核验 | 按开发数据库权威入口只读确认目标连接，再决定是否执行幂等初始化 |
| T1.3 真实资产与 OSS 核对 | `NOT_STARTED` | 初始化脚本不会创建人物/声音媒体 | 确认一组当前用户 `ready` 人物、`origin` 声音和对应 OSS 对象 |
| T1.4 后端构建与启动 | `NOT_STARTED` | 8080 不可用；建议 18080 | T1.1～T1.3 通过后在 18080 启动用户 API |
| T1.5 无 Mock 前端与权限 | `NOT_STARTED` | 尚无当前运行证据 | 启动 Web，验证登录、创作权限和真实素材列表 |
| T1.6 单次真实黄金样例 | `NOT_STARTED` | 尚未获得当前运行证据 | Provider、额度和样例确认后只提交一次固定意图 |
| T1.7 两套任务与项目证据 | `NOT_STARTED` | 尚无任务 ID | 记录 `av_dh_generation_job`、`av_ai_task`、project、asset 和时间线 |
| T1.8 MP4 与工作流验收 | `NOT_STARTED` | 尚无 MP4/工作流哈希 | ffprobe、SHA-256、人工画面检查并导出实际 ComfyUI 工作流 |
| T1.9 固化验收记录 | `NOT_STARTED` | `docs/evidence/T1/` 尚无运行记录 | 写入脱敏证据；未满足则保持未完成 |
| T1.10 独立真实性复核 | `NOT_STARTED` | 尚无非实施者复核 | 复核任务因果链、环境标签、模型声明、未验证项和秘密脱敏后才能 `DONE` |

## 当前已知风险与前置缺口

- 当前命令环境找不到 Java 21；这是后端构建/启动硬前置。
- IndexTTS2 `39000` 与 ComfyUI `8189` 当前未监听；真实声音和底片无法立即生成。
- `8080` 被来源不明进程占用，不得擅自终止；T1 使用 `18080`。
- OSS、DeepSeek、目标数据库迁移、真实 `ready` 人物和 `origin` 声音尚未核验。
- Studio 步骤 0～4 的关键上下文主要在 React 内存，创建 `projectId` 以前刷新会丢任务上下文；T1 运行时不刷新，并记录为后续 Agent 必须修复的恢复边界。
- 当前数字人链使用 `av_dh_generation_job`，时间轴渲染使用 `av_ai_task`；证据必须分别记录，不能假装已经统一。
- 当前仓库无法证明实际 ComfyUI checkpoint；T1 未导出工作流前不能宣称具体 Wan 或 LTX 版本。

这些缺口尚不构成 `BLOCKED`，因为仍有安全的环境、数据和资产清点工作可推进。

## 证据索引

| ID | 支持的验收项 | 环境/真实性 | 源码状态 | 命令或产物 | 结果 |
| --- | --- | --- | --- | --- | --- |
| T0-E01 | 脱敏快照来源与无历史导入 | 本地仓库 | `commit:0238482` | `docs/BASELINE.md` 与 Git 提交 | 基线已建立；不代表 T1 可运行 |
| T0-E02 | 项目迁移到固定 D 盘路径 | 本地仓库 | `commit:cab31a0` | Git 提交与真实项目路径 | 已迁移；不代表 T7 可公开发布 |
| T1-E00 | T1 开工前只读预查 | 本机开发环境 | `HEAD:cab31a0; worktree:clean` | 端口、命令、配置和源码链路清点 | 仅形成前置缺口，不能证明黄金链通过 |

## 本轮交接

- 已完成：参赛设计、决策、阶段路线、进度协议和 T0～T7 验收卡已经固化。
- 未完成/未验证：没有调用真实 Provider，没有真实任务 ID，没有当前基线最终 MP4；T1 不能标记为完成。
- 当前风险：真实环境、资产、模型工作流和两套异步任务链尚未形成同一条证据链。
- 下一条准确动作：按 T1 计划执行 T1.1，只做 JDK/端口/FFmpeg/字体/数据库/Redis 的安全预检；不要终止 8080 未知进程，也不要调用付费 Provider。
- 需要用户决定：实际进入 T1.6 前，确认允许使用的测试账号、固定素材、额度上限以及 IndexTTS2/ComfyUI 的启动方式。

## 开工与收工协议

每次开始施工：确认绝对路径、分支、HEAD 和 `git status`；读取 `PLAN.md`、本文件和当前任务卡；HEAD 或相关配置变化时先判断旧证据是否需转 `NEEDS_REVALIDATION`。

每次结束施工：检查 diff；运行任务卡规定的检查和真实边界证明；把 `PASS`、`FAIL`、`NOT_RUN` 都写入证据；更新下一条准确动作。AI 代码总结、静态阅读、Mock 或“看起来正常”不能单独支撑 `DONE`。
