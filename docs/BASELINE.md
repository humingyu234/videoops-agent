# 代码基线与迁移记录

## 当前快照

- 来源：公司 Gitee 仓库最近一次本机已获取的 `origin/main`。
- 提交：`6ef783d3cc2ef83b420a52080e40b1eff39203e1`。
- 提交时间：2026-08-13 06:27（Asia/Shanghai）。
- 提交主题：`!19 feat(发现): 完成RunningHub单执行配置`。
- 导入方式：`git archive` 导出工作树，不携带原 `.git`、分支和提交历史。

2026-08-14 创建新项目时，Gitee 连接因本机代理/网络错误失败，无法重新确认远端当天是否还有更新。因此上述提交是可验证的缓存基线，不应表述为“Gitee 当前绝对最新”。网络恢复后，应在原仓库 fetch、比较新提交并以显式迁移提交同步，不能覆盖本仓库历史。

## 参赛仓库迁移验收

- `02384829b273a5c060147d0f013bd9a37ac00b42`：导入不携带公司 Git 历史的脱敏快照。
- `cab31a0d16fa3f2a1a2f65d9fbff9fd8d117905a`：将独立仓库迁移到固定路径 `D:\project\videoops-agent`。
- 验收时间：2026-08-14。该验收只证明来源、脱敏和迁移可追溯，不证明真实部署、Provider、最终 MP4 或公开发布门禁已经通过。

## 用户形象分支

- 缓存引用：`origin/codex/avatar-space-provider-router`。
- 提交：`c15f55b`。
- 状态：相对该分支的 merge base，落后主线 27 个提交、领先 1 个提交；未导入本仓库。
- 原因：直接合并会冲突于当前任务、资产、事务、枚举和配置；旧 SQL 也不兼容主线新增任务类型。
- 迁移策略：后续只在当前主线重新实现“生成候选→私有资产→签名预览→人工批准→保存形象”的最小闭环，不 cherry-pick 旧任务引擎、SQL 或开发配置。

## 已执行的公开化处理

- 移除原 Git 历史，避免把历史凭据带入新仓库。
- 将 YAML 与 dotenv 中敏感配置改为环境变量引用或空示例。
- 将配置中的私有运行时 IPv4 地址替换为本地占位地址。
- 新增 `.gitignore` 规则，排除本地环境文件、证书和密钥文件。
- 保留 `scripts/sanitize-public-snapshot.ps1` 作为可重复的快照清理工具；脚本只允许作用于本项目的固定目录。

## 仍需注意

- 快照脱敏不等于原凭据已失效；原项目中出现过的凭据应在提供方独立轮换/吊销。
- 推送公开 GitHub 前必须重新扫描已暂存内容，并人工检查示例、测试夹具、文档和构建脚本。
- 当前快照尚未证明可从零部署或完成真实端到端运行；这属于 T1，而不是 T0 的完成声明。
