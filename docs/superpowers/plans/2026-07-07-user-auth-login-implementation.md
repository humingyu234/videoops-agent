# 用户端登录与 ai_user 管理实现计划

> **已废止，禁止执行。** 本计划中的 `ai_user + userType + 默认 StpUtil/LoginHelper`（用户表加用户类型加默认登录工具）实现方案，已被 `docs/superpowers/specs/2026-07-28-say-requirements-copy-generation-design.md` 的 `P0-A-identity-security`（账号与安全底座）完整替代；本文仅供历史追溯，不得作为实现依据。

## 历史归档说明

原计划中的待办、实施步骤、代码示例、接口约定和验收要求均已移除，避免旧的 `ai_user`（旧用户表）、`userType`（旧用户类型）及默认登录工具方案被误认为仍在执行。当前身份与安全工作以 `P0-A-identity-security` 冻结规格及其配套实施计划为准。
